;;#lang racket

;;---------------------------------------------------------------------
;; record-case
;;(require (planet dvanhorn/record-case:1:1/record-case))


;; gensym in racket ?
;; property lists on symbols ?? put

(module test (hello ht)
  (import scheme)
  (import srfi-69)
  

  (define hello (lambda () "hello world!"))
  (define ht (make-hash-table))

  

  
)











